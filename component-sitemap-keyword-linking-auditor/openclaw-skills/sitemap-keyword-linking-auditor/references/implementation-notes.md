# Implementation Notes

This skill audits and maps opportunities only. It should not edit pages.

Implementation handoff options:

- Use `internal-link-builder-priority` for one target/money page.
- Use `internal-link-builder-sitewide` for approved batches.
- Use `cms-draft-implementer` to make approved CMS/WordPress draft edits.

If On-Page.ai internal link suggestions conflict with keyword-anchor findings, prefer the safer path:

1. Existing keyword mention in relevant main content.
2. Source and target relationship matches architecture hierarchy.
3. No duplicate existing main-content link.
4. Anchor is natural, not forced exact-match.
5. Reviewer approves.
